# Creating new Webpack project

- Create new project with:

```bash
yarn init # and follow the prompts
yarn add -D webpack webpack-cli
```

- Make sure package is private to prevent publishing

- to be continued...